let admin,createThumbnail;_8db‍.x([["default",()=>_8db‍anonymous]]);_8db‍.w("firebase-admin",[["default",["admin"],function(v){admin=v}]]);_8db‍.w("firebase-function-tools/lib/thumbnail",[["default",["createThumbnail"],function(v){createThumbnail=v}]]);


               async function _8db‍anonymous(object) {
  const thumbnail = await createThumbnail(object)

  if (thumbnail) {
    const { fileDir, downloadURL } = thumbnail

    await admin
      .database()
      .ref(fileDir)
      .update({ thumbnail: downloadURL })
  }

  return
}
